{
  "_comment": "Service Account credential (recent format). Download from the IndyKite Hub or ConfigClient.create_service_account_credential(). The 'token' is sent as the Authorization: Bearer header to the Config API.",
  "serviceAccountId": "gid:example-service-account-id",
  "token": "ik1_..."
}
